{{#each posts as |post|}}
  <a
    class="list-group btn list-item main-list single-post"
    href="/posts/{{post.id}}"
  >
    <span class="float-left">{{post.title}}</span>
    <p></p>
    <span class="float-right">Posted by: {{user.name}}</span>
  </a>
{{/each}}